spring:
  application:
    name: ms-franchise
  webflux:
    base-path: /api/v1

server:
  port: 8080

aws:
  region: us-east-2
  endpoint: ${AWS_DYNAMODB_ENDPOINT:#{null}}

dynamodb:
  table-name: franchise-system